{
    "name": "CIFR10",
    "trainer": {
        "epochs": 300,
        "save_dir": "saved/",
        "save_period": 1,
        "verbosity": 2,
        "monitor": "min val_loss",
        "early_stop": 300,
        "tensorboard": true
    }
}
